# Configuración del servidor
spring.application.name=servicio-usuarios
server.port=8081

# Configuración de la base de datos PostgreSQL
# Usuarios y reservas comparten la MISMA base de datos
spring.datasource.url=jdbc:postgresql://localhost:5432/db_cinemax_usuarios
spring.datasource.username=postgres
spring.datasource.password=123456
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Inicializar datos (opcional)
# spring.sql.init.mode=always
# Comentado temporalmente hasta que agregues el script SQL
